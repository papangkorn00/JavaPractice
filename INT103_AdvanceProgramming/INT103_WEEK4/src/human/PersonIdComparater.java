package human;

import java.util.Comparator;

public class PersonIdComparater implements Comparator<Person> {
    @Override
    public int compare(Person o1, Person o2) {
        if(01 == null) &&
        return o1.id - o2.id;
    }
}
